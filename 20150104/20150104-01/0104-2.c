//按照插入排序法由小到大排序
/*#include<stdio.h>
#include<stdlib.h>
#include<math.h>
int main()
{
	int a[10];
	int i,j ,k;
	printf("输入十个数 用空格隔开 \n");
	for(i=0;i<10;i++)
		scanf("%d",&a[i]);
	for(i=1;i<10;i++)
     {
        k=a[i];
        j=i-1;
       while((j>=0)&&(a[j]>k)) 
	   {
       a[j+1]=a[j];
       j--;
        }
       a[j+1]=k;
      }
   printf("你输入的数字按照插入排序法由小到大排序后为\n");
   for(i=0;i<10;i++)
      printf("%d",a[i]);
   system("pause");
   return 0;

}
*/







